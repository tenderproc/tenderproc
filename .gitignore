# dependencies
/node_modules

# next.js build output
/.next/
/out/

# env files (never commit secrets)
.env
.env.local
.env*.local
.env.paddle-ids

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor/os
.DS_Store
*.pem

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# personal Claude Code settings (permissions, etc.) — not shared
.claude/settings.local.json

.vercel
.env*

# playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/

# QA test-account sessions (live cookies) — never commit
e2e/.auth/

# QA audit output (screenshots, reports) — large, one-time, not versioned
test-reports/
